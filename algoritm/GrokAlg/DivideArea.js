

function devide(h, w) {
    let min = h > w ? w : h
    let max = h > w ? h : w
    let ares = max / min

    if (String(areas).includes('.')) {
        
    }

    let areas = []

    // if ()

    // return areas.push()
}

console.log(devide(100,80));